



-- DROP TABLE pgpromise;

-- CREATE TABLE pgpromise(

--     id SERIAL NOT NULL PRIMARY KEY,
--     name varchar(20),
--     city varchar(20)
-- );

INSERT INTO pgpromise VALUES (DEFAULT, 'name', 'city')

SELECT * FROM test WHERE name = '${req.body.name}'

SELECT * FROM test WHERE name = 'Veronica'; SELECT * customers--'